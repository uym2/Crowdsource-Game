DELIMITER $$
CREATE PROCEDURE `sp_getObj`(
	IN p_userID int,
	   p_imgID int
)
BEGIN
	SELECT topleft_x, topleft_y, bottomright_x, bottomright_y
	FROM tbl_objmark
	WHERE user_id = p_userID AND img_id=p_imgID;
END$$

DELIMITER ;
